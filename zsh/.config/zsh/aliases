# Aliases for zsh

# Better ls
alias ll="ls -lah"

# Git stuff
alias ga="git add"
alias gc="git commit"
alias gp="git push"

# Directory stack, Index the directories in stack from 1 to 9 and alias them to move easily
alias d="dirs -v"
for index ({1..9}) alias "$index"="cd +${index}"; unset index
